# Worker runtime — kept minimal so the image stays small.

runpod>=1.7,<2
fastapi>=0.111,<1
uvicorn>=0.29,<1
httpx>=0.27,<0.28
numpy>=1.26,<2

# STT — both tiers
faster-whisper>=1.0,<2
ctranslate2>=4.4,<5

# TTS
qwen-tts>=0.1.0
torchaudio>=2.4
transformers>=4.45,<5
soundfile>=0.12
pydub>=0.25
