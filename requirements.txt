# Worker runtime — kept minimal so the image stays small. Tier-specific extras are
# layered on by the Dockerfile via build args.

runpod>=1.7,<2
fastapi>=0.111,<1
uvicorn>=0.29,<1
httpx>=0.27,<0.28
numpy>=1.26,<2

# STT — both tiers
faster-whisper>=1.0,<2
ctranslate2>=4.4,<5

# TTS fast tier
kokoro>=0.4,<1
soundfile>=0.12
pydub>=0.25

# TTS premium tier (heavy — install via dockerfile arg when building premium)
# torch  -- baked in by the CUDA base image
# transformers>=4.45,<5
# qwen-tts>=0.0.5
